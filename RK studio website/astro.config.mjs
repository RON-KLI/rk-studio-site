// @ts-check
import { defineConfig } from 'astro/config';

// IMPORTANT: change `site` to your real domain before launch.
export default defineConfig({
  site: 'https://ronklimovsky.com',
  build: { format: 'directory' },
});
